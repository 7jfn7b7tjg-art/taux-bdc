# macOS SwiftUI — build notes (optional CI later)
# Requires full Xcode on the runner. For now, build locally after installing Xcode.
# Windows continues to use .github/workflows/release.yml (Python / PyInstaller).
